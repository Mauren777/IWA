{% extends 'layout.twig' %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-8">         
            <section class="articles">
                <div class="article mb-5">
                    <h3>{{ page.title }}</h3>
                    {{ page.body }}
                </div>
            </section>
        </div>
        <div class="col-md-4">
            {% include 'sidebar.twig' %}
        </div>
    </div>
</div>
{% endblock %}